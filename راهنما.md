# راهنمای راه‌اندازی ربات تحلیل خبر + فنی رمزارز

## چیکار می‌کنه؟

1. هر ۱۰ دقیقه (قابل تنظیم) اخبار داغ رمزارز رو چک می‌کنه.
2. اگه خبری مهم بود (بر اساس کلمات کلیدی یا امتیاز محبوبیت) و به یکی از ارزهای تحت‌نظر مربوط بود:
   - قیمت، RSI، ATR، حمایت/مقاومت، هدف، استاپ تقریبی، نسبت ریسک‌به‌ریوارد و افق زمانی تقریبی رو حساب می‌کنه
   - یه کد یکتا (مثل `NWS-001`) به تحلیل می‌ده و توی تلگرام برات می‌فرسته
3. تو با فرستادن `بررسی معامله NWS-001 1000 2` (یا `/check NWS-001 1000 2`) می‌گی سرمایه‌ت ۱۰۰۰ دلار و ریسکت ۲٪ هست.
4. ربات حجم پوزیشن مناسب رو با فرمول استاندارد مدیریت ریسک برات حساب می‌کنه.

⚠️ **این ربات فقط اطلاعات و محاسبات ریاضی می‌ده. تصمیم نهایی، انتخاب اهرم، و مسئولیت هر معامله همیشه با خودته.**

---

## ۱. پیش‌نیازها

### الف) ساخت ربات تلگرام
1. به [@BotFather](https://t.me/BotFather) پیام بده → `/newbot` → یه اسم انتخاب کن.
2. توکن رو یادداشت کن (شبیه: `123456789:ABCdefGhIJKlmNoPQRstuVwxyZ`)
3. Chat ID خودت رو پیدا کن:
   - به ربات خودت یه پیام (مثلاً `سلام`) بده
   - در مرورگر: `https://api.telegram.org/bot<TOKEN>/getUpdates`
   - عدد جلوی `"chat":{"id":` رو بردار

### ب) گرفتن کلید CoinStats (رایگان)
1. برو به [openapi.coinstats.app](https://openapi.coinstats.app) و با ایمیل/گوگل ثبت‌نام کن (رایگانه، بدون کارت بانکی — ۲۰,۰۰۰ کردیت رایگان در ماه).
2. بعد از ورود، از داشبورد یه **API Key** بساز و کپیش کن.

## ۲. نصب

```bash
cd crypto_bot
pip install -r requirements.txt

export TELEGRAM_BOT_TOKEN="توکن ربات تلگرامت"
export TELEGRAM_CHAT_ID="چت آی‌دی خودت"
export COINSTATS_API_KEY="کلید کوین‌استتس"

python3 main.py
```

روی ویندوز به‌جای `export` از `set` استفاده کن.

## ۳. استفاده

- ربات خودش پیام‌های خبر+تحلیل رو خودکار می‌فرسته، کاری نکن.
- زیر هر پیام تحلیل، یه کد هست (مثل `NWS-003`). برای محاسبه‌ی حجم پوزیشن بفرست:

```
بررسی معامله NWS-003 1000 2
```

یا:

```
/check NWS-003 1000 2
```

(۱۰۰۰ = سرمایه‌ت به دلار، ۲ = درصد ریسکی که حاضری تو این معامله بپذیری)

## ۴. شخصی‌سازی

توی `config.py`:
- `WATCHED_COINS`: لیست ارزهایی که دنبال می‌کنه (می‌تونی هر جفت‌ارز Binance اضافه کنی)
- `NEWS_CHECK_INTERVAL_MINUTES`: هر چند دقیقه چک کنه
- `MIN_VOTES_THRESHOLD`: چقدر خبر باید محبوب باشه تا "مهم" حساب بشه
- `IMPORTANT_KEYWORDS` (در `news.py`): کلمات کلیدی که نشونه‌ی خبر مهمن

## ۵. اجرای ۲۴ ساعته روی سرور

همون‌طور که قبلاً گفتیم، این رو روی همون سروری که برای Vendis می‌گیری هم می‌شه گذاشت — یا با Docker جدا (پیشنهاد اصلی، هماهنگ با استکی که برای Vendis داری) یا با یه سرویس `systemd` جدا. اگه خواستی این بخش رو هم برات آماده کنم بگو.

## نکته امنیتی

هیچ‌وقت توکن‌ها رو مستقیم توی کد ننویس یا جایی مثل گیت‌هاب پابلیک آپلود نکن — همیشه از متغیر محیطی استفاده کن (همون‌طور که در این پروژه انجام شده).
